source "https://rubygems.org"

# Matches the gem set GitHub Pages actually builds with, so a local
# `bundle exec jekyll serve` behaves the same as the live site.
# This file is only for local preview — GitHub's own Pages build
# ignores it and uses its own pinned versions.
gem "github-pages", group: :jekyll_plugins
